# Rust callers — `cpal-kotlin`

> Generated by `scaffold/generate_caller_reports.py` from `workspace_dep_graph.json` (Cargo-aware dep graph). Do not hand-edit. Re-run after the graph is rebuilt.

## What this file is

For every Rust crate this repo owns (under `tmp/`), this file lists every **cross-repo** workspace caller: every `use` statement in another `*-kotlin` repo's Rust source that imports a symbol from one of this repo's crates. Symbols are grouped per-crate; codex-kotlin sites are surfaced first per symbol (codex is the priority consumer driving the port queue).

**Granularity:** import-level (each row is one `use` line). Call-site detail — where the imported name is actually invoked, implemented, pattern-matched, or used as a generic bound inside the caller body — requires a per-symbol grep across the listed caller files. The graph tells you exactly which files to grep.

**Cross-repo only.** Imports between crates inside this same repo are omitted — those are intra-repo refactoring concerns, not the cross-project porting concern this file exists to surface.

**Cargo-resolved.** Counts here honor `package = "..."` renames, `workspace = true` inheritance, and `[target.<cfg>.dependencies]`. Stdlib (`std`, `core`, `alloc`) and intra-crate sub-module references are excluded.

## Crates in this repo

| package | module | inbound use sites | distinct caller crates | distinct caller repos | codex sites |
|---|---|---:|---:|---:|---:|
| `cpal` | `cpal` | 25 | 2 | 2 | 4 |

## `cpal`

### Inbound symbols (11 distinct, 25 total `use` sites)

Sorted by codex demand, then total demand. The first symbols are the priority queue for porting `cpal`.

#### `cpal::traits::DeviceTrait` — 5 site(s) (2 codex, 3 other, 2 caller crates)

- ✅ `codex-kotlin/codex-tui` `src/audio_device.rs:2` — `use cpal::traits::DeviceTrait`
- ✅ `codex-kotlin/codex-tui` `src/voice.rs:5` — `use cpal::traits::DeviceTrait`
- `libwebrtc-kotlin/local_audio` `src/audio_capture.rs:2` — `use cpal::traits::DeviceTrait`
- `libwebrtc-kotlin/local_audio` `src/audio_playback.rs:3` — `use cpal::traits::DeviceTrait`
- `libwebrtc-kotlin/local_audio` `src/main.rs:11` — `use cpal::traits::DeviceTrait`

#### `cpal::traits::StreamTrait` — 3 site(s) (1 codex, 2 other, 2 caller crates)

- ✅ `codex-kotlin/codex-tui` `src/voice.rs:6` — `use cpal::traits::StreamTrait`
- `libwebrtc-kotlin/local_audio` `src/audio_capture.rs:2` — `use cpal::traits::StreamTrait`
- `libwebrtc-kotlin/local_audio` `src/audio_playback.rs:3` — `use cpal::traits::StreamTrait`

#### `cpal::traits::HostTrait` — 2 site(s) (1 codex, 1 other, 2 caller crates)

- ✅ `codex-kotlin/codex-tui` `src/audio_device.rs:3` — `use cpal::traits::HostTrait`
- `libwebrtc-kotlin/local_audio` `src/main.rs:11` — `use cpal::traits::HostTrait`

#### `cpal::Device` — 3 site(s) (0 codex, 3 other, 1 caller crates)

- `libwebrtc-kotlin/local_audio` `src/audio_capture.rs:3` — `use cpal::Device`
- `libwebrtc-kotlin/local_audio` `src/audio_playback.rs:4` — `use cpal::Device`
- `libwebrtc-kotlin/local_audio` `src/main.rs:12` — `use cpal::Device`

#### `cpal::StreamConfig` — 3 site(s) (0 codex, 3 other, 1 caller crates)

- `libwebrtc-kotlin/local_audio` `src/audio_capture.rs:3` — `use cpal::StreamConfig`
- `libwebrtc-kotlin/local_audio` `src/audio_playback.rs:4` — `use cpal::StreamConfig`
- `libwebrtc-kotlin/local_audio` `src/main.rs:12` — `use cpal::StreamConfig`

#### `cpal::SampleFormat` — 2 site(s) (0 codex, 2 other, 1 caller crates)

- `libwebrtc-kotlin/local_audio` `src/audio_capture.rs:3` — `use cpal::SampleFormat`
- `libwebrtc-kotlin/local_audio` `src/audio_playback.rs:4` — `use cpal::SampleFormat`

#### `cpal::SizedSample` — 2 site(s) (0 codex, 2 other, 1 caller crates)

- `libwebrtc-kotlin/local_audio` `src/audio_capture.rs:3` — `use cpal::SizedSample`
- `libwebrtc-kotlin/local_audio` `src/audio_playback.rs:4` — `use cpal::SizedSample`

#### `cpal::Stream` — 2 site(s) (0 codex, 2 other, 1 caller crates)

- `libwebrtc-kotlin/local_audio` `src/audio_capture.rs:3` — `use cpal::Stream`
- `libwebrtc-kotlin/local_audio` `src/audio_playback.rs:4` — `use cpal::Stream`

#### `cpal::FromSample` — 1 site(s) (0 codex, 1 other, 1 caller crates)

- `libwebrtc-kotlin/local_audio` `src/audio_playback.rs:4` — `use cpal::FromSample`

#### `cpal::Sample` — 1 site(s) (0 codex, 1 other, 1 caller crates)

- `libwebrtc-kotlin/local_audio` `src/audio_playback.rs:4` — `use cpal::Sample`

#### `cpal::SampleRate` — 1 site(s) (0 codex, 1 other, 1 caller crates)

- `libwebrtc-kotlin/local_audio` `src/main.rs:12` — `use cpal::SampleRate`
