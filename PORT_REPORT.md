# Port report — `cpal-kotlin`

> Generated by `scaffold/generate_repo_reports.py` from `workspace_dep_graph.json` ⨯ `port_priority.json`. Do not hand-edit. Re-run after the workspace dep graph is rebuilt.

## Overview

- **1** member crate(s): `cpal`
- **Codex-kotlin distance**: **hop 1** from codex-kotlin via cross-repo Rust deps
- **Inbound**: 25 `use` sites from 2 other repos
- **Outbound**: 27 `use` sites to 2 other repos
- **Unresolved external**: 6 published Cargo crates with no `*-kotlin/` port

## Member crates

| package | module | inbound `use` sites | outbound external repos | Jira | rank | status |
|---|---|---:|---:|---|---:|---|
| `cpal` | `cpal` | 25 | 2 | COD-35 | 271 | To Do |

## Inbound — what to port first

Every symbol this repo's crates need to expose, ranked by total workspace demand. **Codex-kotlin sites are listed first per symbol** because codex is the priority consumer driving the port queue. Anything not listed here is not currently `use`'d by any kotlinmania repo and can be deferred.

### Inbound caller summary (25 edges, 2 caller crates)

| caller repo | caller crate | use sites | codex consumer? |
|---|---|---:|---|
| `libwebrtc-kotlin` | `local_audio` | 21 |  |
| `codex-kotlin` | `codex-tui` | 4 | ✅ |

### Symbol-level demand (11 distinct symbols)

Sorted by codex demand, then total demand. Symbols with codex-kotlin sites are the priority queue. Each symbol entry below shows up to 12 call sites (codex first).

| symbol | total | codex | other | distinct callers |
|---|---:|---:|---:|---:|
| `cpal::traits::DeviceTrait` | 5 | 2 | 3 | 2 |
| `cpal::traits::StreamTrait` | 3 | 1 | 2 | 2 |
| `cpal::traits::HostTrait` | 2 | 1 | 1 | 2 |
| `cpal::Device` | 3 | 0 | 3 | 1 |
| `cpal::StreamConfig` | 3 | 0 | 3 | 1 |
| `cpal::SampleFormat` | 2 | 0 | 2 | 1 |
| `cpal::SizedSample` | 2 | 0 | 2 | 1 |
| `cpal::Stream` | 2 | 0 | 2 | 1 |
| `cpal::FromSample` | 1 | 0 | 1 | 1 |
| `cpal::Sample` | 1 | 0 | 1 | 1 |
| `cpal::SampleRate` | 1 | 0 | 1 | 1 |

#### Per-symbol detail (top 11 symbols)

##### `cpal::traits::DeviceTrait` (5 sites, 2 codex, 3 other, 2 caller crates)

Codex call sites:

- `codex-kotlin/codex-tui` `src/audio_device.rs:2` — `use cpal::traits::DeviceTrait`
- `codex-kotlin/codex-tui` `src/voice.rs:5` — `use cpal::traits::DeviceTrait`

Other workspace call sites:

- `libwebrtc-kotlin/local_audio` `src/audio_capture.rs:2` — `use cpal::traits::DeviceTrait`
- `libwebrtc-kotlin/local_audio` `src/audio_playback.rs:3` — `use cpal::traits::DeviceTrait`
- `libwebrtc-kotlin/local_audio` `src/main.rs:11` — `use cpal::traits::DeviceTrait`

##### `cpal::traits::StreamTrait` (3 sites, 1 codex, 2 other, 2 caller crates)

Codex call sites:

- `codex-kotlin/codex-tui` `src/voice.rs:6` — `use cpal::traits::StreamTrait`

Other workspace call sites:

- `libwebrtc-kotlin/local_audio` `src/audio_capture.rs:2` — `use cpal::traits::StreamTrait`
- `libwebrtc-kotlin/local_audio` `src/audio_playback.rs:3` — `use cpal::traits::StreamTrait`

##### `cpal::traits::HostTrait` (2 sites, 1 codex, 1 other, 2 caller crates)

Codex call sites:

- `codex-kotlin/codex-tui` `src/audio_device.rs:3` — `use cpal::traits::HostTrait`

Other workspace call sites:

- `libwebrtc-kotlin/local_audio` `src/main.rs:11` — `use cpal::traits::HostTrait`

##### `cpal::Device` (3 sites, 0 codex, 3 other, 1 caller crates)

Other workspace call sites:

- `libwebrtc-kotlin/local_audio` `src/audio_capture.rs:3` — `use cpal::Device`
- `libwebrtc-kotlin/local_audio` `src/audio_playback.rs:4` — `use cpal::Device`
- `libwebrtc-kotlin/local_audio` `src/main.rs:12` — `use cpal::Device`

##### `cpal::StreamConfig` (3 sites, 0 codex, 3 other, 1 caller crates)

Other workspace call sites:

- `libwebrtc-kotlin/local_audio` `src/audio_capture.rs:3` — `use cpal::StreamConfig`
- `libwebrtc-kotlin/local_audio` `src/audio_playback.rs:4` — `use cpal::StreamConfig`
- `libwebrtc-kotlin/local_audio` `src/main.rs:12` — `use cpal::StreamConfig`

##### `cpal::SampleFormat` (2 sites, 0 codex, 2 other, 1 caller crates)

Other workspace call sites:

- `libwebrtc-kotlin/local_audio` `src/audio_capture.rs:3` — `use cpal::SampleFormat`
- `libwebrtc-kotlin/local_audio` `src/audio_playback.rs:4` — `use cpal::SampleFormat`

##### `cpal::SizedSample` (2 sites, 0 codex, 2 other, 1 caller crates)

Other workspace call sites:

- `libwebrtc-kotlin/local_audio` `src/audio_capture.rs:3` — `use cpal::SizedSample`
- `libwebrtc-kotlin/local_audio` `src/audio_playback.rs:4` — `use cpal::SizedSample`

##### `cpal::Stream` (2 sites, 0 codex, 2 other, 1 caller crates)

Other workspace call sites:

- `libwebrtc-kotlin/local_audio` `src/audio_capture.rs:3` — `use cpal::Stream`
- `libwebrtc-kotlin/local_audio` `src/audio_playback.rs:4` — `use cpal::Stream`

##### `cpal::FromSample` (1 sites, 0 codex, 1 other, 1 caller crates)

Other workspace call sites:

- `libwebrtc-kotlin/local_audio` `src/audio_playback.rs:4` — `use cpal::FromSample`

##### `cpal::Sample` (1 sites, 0 codex, 1 other, 1 caller crates)

Other workspace call sites:

- `libwebrtc-kotlin/local_audio` `src/audio_playback.rs:4` — `use cpal::Sample`

##### `cpal::SampleRate` (1 sites, 0 codex, 1 other, 1 caller crates)

Other workspace call sites:

- `libwebrtc-kotlin/local_audio` `src/main.rs:12` — `use cpal::SampleRate`

## Outbound — what this repo depends on

Other kotlinmania repos this repo's Rust source `use`s. Symbol-level detail per target lives in **that target repo's PORT_REPORT.md inbound section** (no duplication). Sorted by Jira rank, then by `use`-site count.

| target repo | use sites | distinct symbols | target packages | Jira | rank | status |
|---|---:|---:|---|---|---:|---|
| `clap-kotlin` | 3 | 1 | `clap` | COD-30 | 50 | To Do |
| `windows-kotlin` | 24 | 20 | `windows` | COD-191 | 191 | To Do |

## Unresolved external dependencies

Published Cargo crates this repo's Rust source `use`s but for which no `<crate>-kotlin/` repo exists. Each is either a missing port to file in Jira, or an intentional external dep (system library / FFI / build-only). If a port lives at a non-conventional repo name, add it to `PACKAGE_REPO_OVERRIDES` in `scaffold/build_workspace_dep_graph.py`.

| package | edges | distinct symbols | importing members in this repo |
|---|---:|---:|---|
| `wasm-bindgen` | 24 | 6 | `cpal` |
| `web-sys` | 16 | 15 | `cpal` |
| `js-sys` | 14 | 10 | `cpal` |
| `dasp_sample` | 6 | 6 | `cpal` |
| `wasm-bindgen-futures` | 3 | 2 | `cpal` |
| `ringbuf` | 1 | 1 | `cpal` |
